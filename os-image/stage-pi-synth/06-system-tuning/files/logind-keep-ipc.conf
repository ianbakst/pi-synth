[Login]
# Do not destroy POSIX shared memory / semaphores when a user's last login
# session ends. See the long comment in ../00-run.sh — without this, every SSH
# login+logout as 'synth' silently guts the running JACK server.
RemoveIPC=no
