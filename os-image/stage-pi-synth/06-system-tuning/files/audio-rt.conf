# Realtime scheduling + memory-locking grants for the audio group.
# Services set their own LimitRTPRIO/LimitMEMLOCK; this covers interactive/login
# sessions (e.g. running jackd/cyclictest by hand over SSH). synth is in 'audio'.
@audio - rtprio 99
@audio - memlock unlimited
